# AGENTS

## Environment Note

- 当前 Codex 所在机器不是实际部署机。
- 本地仓库可用于阅读代码、修改文件、准备排障方案，但不能假设能直接访问部署机上的 Docker、数据库、日志文件或运行中的服务。

## Troubleshooting Workflow

- 当需要部署机上的运行时信息时，优先给出可直接执行的命令。
- 由用户在部署机上执行命令，并把输出结果返回给 Codex。
- 如果需要进一步排查，继续基于返回结果给出下一批最小必要命令，避免一次性索取过多信息。

## Command Guidelines

- 命令要尽量短、可复制、上下文明确。
- 优先提供只读排查命令，例如 `docker compose logs`、`docker compose ps`、`cat`、`sed`、`tail`、`grep`、`mysql` 查询。
- 如果命令依赖工作目录、容器名、环境变量或时间窗口，要在命令前说明。
- 除非用户明确要求，否则不要默认执行破坏性操作，例如重建数据卷、删除容器、清库或覆盖配置。

## Expected Response Style

- 先说明要确认什么，再给命令。
- 收到用户贴回的输出后，先提炼关键现象，再判断根因和下一步。
- 如果当前环境无法直接验证，要明确说明哪些判断是基于代码推断。
